// Plain-language explanations layered on top of concepts.js and cases.js:
// an everyday analogy, What / Why / How, key terms, extra scenario questions and references.

export const CONCEPT_EXTRA = {
  scalability: {
    analogy: 'A food stall with one cook. When the queue grows you can hire a faster cook (scale up) or open more counters with more cooks (scale out). More counters only works if any cook can serve any customer — nobody’s order is stuck in one cook’s head.',
    what: 'Scalability is how well a system keeps working — fast and correctly — as the number of users, requests or data grows.',
    why: 'Traffic grows, and it spikes (sales, viral posts, news). A system that works for 1,000 users can fall over at 100,000 if it wasn’t designed to grow.',
    how: 'Keep app servers stateless, put them behind a load balancer, and add more when load rises. Then fix the next bottleneck — usually the database — with caching, replication and sharding.',
    terms: [['Vertical scaling', 'Making one machine bigger (more CPU / RAM).'], ['Horizontal scaling', 'Adding more machines that share the work.'], ['Stateless', 'The server keeps no user data between requests, so any server can handle any request.'], ['Bottleneck', 'The one part that limits the whole system’s capacity.']],
    quiz: [
      { q: 'Your app keeps login sessions in each server’s memory. You add a second server behind a load balancer and users are randomly logged out. Why?', options: ['The load balancer is broken', 'Each server has its own memory, so a request landing on the other server finds no session', 'The database is slow', 'Cookies expired'], answer: 1, why: 'State in server memory breaks horizontal scaling. Move sessions to a shared store like Redis, or use signed tokens.' },
      { q: 'After adding lots of stateless web servers, what usually becomes the bottleneck next?', options: ['The load balancer’s CPU', 'The shared database', 'DNS', 'The CDN'], answer: 1, why: 'Every server still talks to the same database; it becomes the single point everyone waits on.' },
      { q: 'Autoscaling adds servers when CPU is high. What must be true for it to work smoothly?', options: ['Servers are stateless and start quickly', 'Servers share one disk', 'Users are pinned to one server', 'The database runs in memory'], answer: 0, why: 'New servers must be able to take any request immediately.' },
    ],
    refs: ['ddia', 'primer', 'sdi1'],
  },
  'latency-throughput': {
    analogy: 'A motorway: latency is how long one car takes to drive from A to B; throughput is how many cars pass per hour. Adding lanes raises throughput without making any single car faster.',
    what: 'Latency is how long one request takes. Throughput is how many requests the system handles per second. Percentiles (p50, p99) describe the spread of latencies.',
    why: 'Users feel latency — every 100 ms matters. Businesses pay for throughput. And averages hide the slow requests that make users give up.',
    how: 'Measure p50/p95/p99, not just averages. Cut serial network hops, do independent calls in parallel, cache, and set timeouts. Use Little’s law to size pools and queues.',
    terms: [['Latency', 'Time for one request, usually in milliseconds.'], ['Throughput', 'Requests completed per second (QPS / RPS).'], ['p99', 'The latency that 99% of requests are faster than — the “slow tail”.'], ['Tail latency', 'The slowest few percent of requests.'], ['Little’s law', 'In-flight requests = throughput × latency.']],
    quiz: [
      { q: 'A page calls 10 backend services one after another, each taking 50 ms. Roughly how long does the page take?', options: ['50 ms', '100 ms', '500 ms', '5 s'], answer: 2, why: 'Sequential calls add up: 10 × 50 ms = 500 ms.' },
      { q: 'If those 10 calls run in parallel instead, the page takes roughly…', options: ['The sum of all calls', 'As long as the slowest single call', 'The average call', 'Zero'], answer: 1, why: 'In parallel you wait only for the slowest one — which is why tail latency matters so much.' },
      { q: '“p99 latency = 800 ms” means…', options: ['99% of requests take exactly 800 ms', '99% of requests finish within 800 ms; 1% are slower', 'The average is 800 ms', 'The maximum is 800 ms'], answer: 1, why: 'Percentiles describe the distribution: p99 is the boundary of the slowest 1%.' },
    ],
    refs: ['tail', 'little', 'ddia'],
  },
  availability: {
    analogy: 'A shop that promises to be open 99.9% of the time may be shut for about 8.8 hours a year. Keep a second key-holder (a replica) and the shop opens even when one person is sick.',
    what: 'Availability is the share of time a system works for users. It’s measured in “nines”: 99.9%, 99.99%…',
    why: 'Downtime loses money and trust. And every component you depend on can fail, so failures must be expected, not hoped away.',
    how: 'Remove single points of failure by running at least two of everything, spread across zones. Fail over automatically, avoid long chains of hard dependencies, and track reliability with SLOs and error budgets.',
    terms: [['SPOF', 'Single point of failure — one part whose failure takes everything down.'], ['Failover', 'Automatically switching to a standby when the primary dies.'], ['SLO', 'An internal reliability target, e.g. 99.9% of requests succeed.'], ['SLA', 'A contractual promise to customers, with penalties.'], ['Error budget', 'The allowed failure: 100% − SLO.']],
    quiz: [
      { q: 'You run one database server. Which change most improves availability?', options: ['A bigger CPU', 'A standby replica with automatic failover', 'More indexes', 'A CDN'], answer: 1, why: 'Redundancy removes the single point of failure.' },
      { q: '99.99% availability allows about how much downtime per year?', options: ['5 minutes', '52 minutes', '8.8 hours', '3.6 days'], answer: 1, why: '0.01% of 525,600 minutes ≈ 52.6 minutes.' },
      { q: 'Why is an SLA usually looser than the internal SLO?', options: ['Lawyers prefer round numbers', 'To leave a safety margin before penalties kick in', 'SLOs only apply to databases', 'There’s no reason'], answer: 1, why: 'You aim high internally (SLO) and promise a bit less externally (SLA).' },
    ],
    refs: ['sreSlo', 'sre', 'primer'],
  },
  estimation: {
    analogy: 'Planning a party: 50 guests × 3 drinks each ≈ 150 drinks. You don’t need the exact number — you need to know whether to buy a crate or a truckload.',
    what: 'Back-of-the-envelope estimation is quick maths to find the scale of a system: requests per second, storage, bandwidth and memory.',
    why: 'Numbers decide the design. 10 requests/s fits on one server; 1 million/s needs caches, sharding and queues. Estimating shows you’re designing for reality.',
    how: 'Start from daily active users × actions per user. Divide by ~100,000 seconds a day to get QPS; multiply by 2–3 for peak. Storage = items per day × size × retention × replicas. Round aggressively.',
    terms: [['DAU', 'Daily active users.'], ['QPS', 'Queries (requests) per second.'], ['Peak factor', 'How much busier the busiest hour is than average (usually 2–3×).'], ['Working set', 'The data that’s actively used — what a cache should hold.']],
    quiz: [
      { q: '10 million users each upload 2 photos a day at 500 KB each. New storage per day?', options: ['1 GB', '10 GB', '10 TB', '1 PB'], answer: 2, why: '10M × 2 × 500 KB = 10¹³ bytes = 10 TB.' },
      { q: 'Peak traffic is 50,000 requests/s and one server handles 2,000/s. How many servers (no headroom)?', options: ['5', '25', '250', '2,500'], answer: 1, why: '50,000 ÷ 2,000 = 25. In practice add ~30–50% headroom.' },
      { q: 'Why round aggressively when estimating?', options: ['Interviewers dislike decimals', 'You only need the order of magnitude to choose an architecture', 'Calculators are banned', 'To look confident'], answer: 1, why: 'Whether it’s 1,160 or 1,000 QPS doesn’t change the design; 1K vs 1M does.' },
    ],
    refs: ['sdi1', 'latency', 'primer'],
  },
  'load-balancing': {
    analogy: 'A host at a busy restaurant seating guests at different tables, so no single waiter is overwhelmed — and never seating anyone at a table whose waiter has gone home.',
    what: 'A load balancer sits in front of many servers and decides which one handles each incoming request.',
    why: 'It lets you add servers to handle more traffic, and it keeps the site up when a server dies by sending traffic only to healthy ones.',
    how: 'Clients connect to one address (the load balancer). It picks a server with an algorithm (round robin, least connections, hashing), health-checks servers every few seconds, and removes failed ones.',
    terms: [['Health check', 'A periodic probe (e.g. GET /health) to see whether a server is alive.'], ['L4 load balancer', 'Routes network connections by IP/port; very fast, doesn’t read HTTP.'], ['L7 load balancer', 'Reads HTTP, so it can route by URL path or header.'], ['Sticky session', 'Sending the same user to the same server every time.']],
    quiz: [
      { q: 'A server crashes. How does the load balancer know to stop sending it traffic?', options: ['It doesn’t', 'Health checks fail, so it’s removed from the pool', 'DNS tells it', 'The server emails it'], answer: 1, why: 'Failed health checks eject the server until it recovers.' },
      { q: 'Which approach keeps the same key on the same cache server, improving hit rates?', options: ['Round robin', 'Random', 'Consistent hashing on the key', 'Least connections'], answer: 2, why: 'Hash-based routing sends a given key to the same server every time.' },
      { q: 'The load balancer is itself one machine. What’s the risk and the fix?', options: ['No risk', 'It’s a single point of failure — run a redundant pair with failover', 'Scale it vertically', 'Switch to UDP'], answer: 1, why: 'Load balancers are deployed in active/passive or active/active pairs.' },
    ],
    refs: ['cfLb', 'primer', 'sdi1'],
  },
  caching: {
    analogy: 'Keeping the books you read most on your desk instead of walking to the library each time. The desk is small, so you keep the popular ones — and you must notice when a book has a new edition.',
    what: 'A cache is a small, very fast store (usually in memory) that keeps copies of frequently used data.',
    why: 'Memory is ~100× faster than a database query. Caching cuts latency and takes most of the read load off the database.',
    how: 'On a read, check the cache first; on a miss, load from the database and store it with an expiry (TTL). When data changes, delete the cached copy. When the cache is full, evict the least-used items (LRU/LFU).',
    terms: [['Hit / miss', 'Found in cache / not found (must go to the database).'], ['Hit rate', 'The share of reads served by the cache.'], ['TTL', 'Time-to-live: how long an entry stays before it expires.'], ['Eviction', 'Removing entries when the cache is full.'], ['Invalidation', 'Removing or updating cached data after the source changes.'], ['Stampede', 'Many requests missing at once and all hitting the database.']],
    quiz: [
      { q: 'A user updates their profile photo but keeps seeing the old one. Most likely cause?', options: ['The database lost the write', 'A stale cached copy wasn’t invalidated and hasn’t expired yet', 'A load balancer bug', 'The CDN is down'], answer: 1, why: 'Classic cache invalidation problem: delete the cache entry on update, or use short TTLs.' },
      { q: 'Why delete the cache entry after a database update instead of writing the new value into it?', options: ['Deleting is faster', 'It avoids races where an older value overwrites a newer one', 'Caches can’t be updated', 'It saves memory'], answer: 1, why: 'Two concurrent updates can land in the cache out of order; deleting forces a fresh read.' },
      { q: 'Hit rate 95%, cache takes 1 ms, database 50 ms. Average read latency is about…', options: ['1 ms', '3.5 ms', '25 ms', '50 ms'], answer: 1, why: '0.95 × 1 + 0.05 × (1 + 50) ≈ 3.5 ms. A small miss rate dominates the average.' },
    ],
    refs: ['awsCaching', 'memcache', 'redisEvict'],
  },
  cdn: {
    analogy: 'Instead of one warehouse in America shipping to the whole world, a store chain keeps popular products in local shops in every city.',
    what: 'A Content Delivery Network is a worldwide network of servers (“edges”) that keep copies of your files close to users.',
    why: 'Distance costs time: a round trip across the ocean is ~150 ms. Edges cut that to a few ms, and they absorb most of the traffic so your servers don’t have to.',
    how: 'Users are routed to the nearest edge. If it has the file, it serves it immediately; if not, it fetches it once from your origin and caches it according to your Cache-Control headers.',
    terms: [['Edge', 'A CDN server near users.'], ['Origin', 'Your own server or storage — the source of truth.'], ['Cache-Control', 'HTTP header saying how long a response may be cached.'], ['Purge', 'Forcing the CDN to drop cached copies.']],
    quiz: [
      { q: 'Which is the WORST thing to cache on a public CDN?', options: ['The site logo', 'The JavaScript bundle', 'A user’s private bank balance', 'A video segment'], answer: 2, why: 'Personal, frequently changing data must not be served to other users from a shared cache.' },
      { q: 'Users in India see slow image loads from a server in the US. Best fix?', options: ['A bigger US server', 'A CDN with edges near the users', 'Compress the HTML', 'Add a database replica'], answer: 1, why: 'The problem is distance; edges move the content closer.' },
      { q: 'What does `Cache-Control: max-age=31536000, immutable` mean?', options: ['Never cache', 'Cache for a year and don’t even check for updates', 'Cache for one minute', 'Delete after one use'], answer: 1, why: 'Safe only for versioned file names like app.3f9a.js.' },
    ],
    refs: ['cfCdn', 'mdnCaching', 'openconnect'],
  },
  'consistent-hashing': {
    analogy: 'Seats around a round table. Each guest (key) sits and walks clockwise to the nearest waiter (server). If a waiter leaves, only the guests who walked to them move — to the next waiter along.',
    what: 'Consistent hashing is a way to spread keys across servers so that adding or removing a server only moves a small share of keys.',
    why: 'With simple `hash % N`, changing N reshuffles almost every key — a cache would suddenly miss nearly everything, and a database would move almost all its data.',
    how: 'Place servers and keys on a circle using a hash. Each key belongs to the first server clockwise. Give each server many “virtual nodes” around the circle so the load spreads evenly.',
    terms: [['Hash ring', 'The circle of hash values (e.g. 0 to 2³²).'], ['Virtual node', 'One of many positions a single physical server takes on the ring.'], ['Rebalancing', 'Moving keys after servers are added or removed.']],
    quiz: [
      { q: 'With `hash % N` and 10 servers, you add 1 more. Roughly what share of keys move?', options: ['~10%', '~50%', '~90%', '0%'], answer: 2, why: 'A key stays only if hash % 10 = hash % 11 — true for only about 1 in 11 keys.' },
      { q: 'On the ring, a key belongs to…', options: ['The nearest server in either direction', 'The first server clockwise from it', 'A random server', 'The server with the most keys'], answer: 1, why: 'That’s the rule that keeps movement local.' },
      { q: 'A server dies. With virtual nodes, its keys go to…', options: ['One neighbouring server', 'Many different servers', 'Nowhere — they’re lost', 'The load balancer'], answer: 1, why: 'Its virtual nodes are scattered, so the load spreads across many survivors.' },
    ],
    refs: ['consistentHashing', 'dynamo', 'ddia'],
  },
  'sql-nosql': {
    analogy: 'SQL is a well-organised filing cabinet with strict forms and cross-references. NoSQL is a set of labelled boxes: quick to add to and easy to spread over many rooms, but you must know which box to look in.',
    what: 'SQL (relational) databases store data in tables with fixed schemas and support joins and transactions. NoSQL databases (key-value, document, wide-column, graph) use other models that trade some of that for scale or flexibility.',
    why: 'The database is the hardest part of a system to change later. Picking the one that fits your access patterns avoids pain for years.',
    how: 'Start relational by default. Choose NoSQL when writes exceed one machine, access is simple lookups by key, the schema varies a lot, or you need multi-region writes. Many systems use both.',
    terms: [['Schema', 'The defined structure of your data (tables, columns, types).'], ['Join', 'Combining rows from several tables in one query.'], ['ACID', 'Atomic, Consistent, Isolated, Durable — the guarantees of transactions.'], ['BASE', 'Basically Available, Soft state, Eventually consistent — typical NoSQL trade-off.']],
    quiz: [
      { q: 'You need “orders with their customer and items”, with flexible filtering and reports. Best fit?', options: ['A relational database with joins', 'A key-value store', 'Blob storage', 'A cache'], answer: 0, why: 'Related entities and ad-hoc queries are what relational databases are built for.' },
      { q: 'Storing 1 million sensor readings per second, queried by sensor and time range. Best fit?', options: ['A single MySQL server', 'A wide-column / time-series store', 'A graph database', 'A spreadsheet'], answer: 1, why: 'Huge sequential writes partitioned by sensor with time ordering suit Cassandra-style stores.' },
      { q: 'What does atomicity (the “A” in ACID) guarantee?', options: ['Data is encrypted', 'A transaction happens completely or not at all', 'Reads are fast', 'The database is always available'], answer: 1, why: 'No half-done transfers: either both the debit and the credit happen, or neither.' },
    ],
    refs: ['ddia', 'bigtable', 'primer'],
  },
  indexing: {
    analogy: 'The index at the back of a book: instead of reading every page to find “Raft”, you look it up alphabetically and jump to page 312.',
    what: 'An index is an extra sorted structure that lets the database find rows without scanning the whole table.',
    why: 'Without an index, finding one user among 50 million means reading all 50 million rows. With one, it takes a handful of lookups.',
    how: 'Create indexes on the columns you filter and sort by. The database keeps them updated on every write — so each index makes writes a little slower and uses disk.',
    terms: [['B-tree', 'A balanced sorted tree — the standard index in SQL databases.'], ['LSM-tree', 'A write-optimised structure: append in memory, flush sorted files, merge later.'], ['Composite index', 'An index on several columns, used left to right.'], ['Full table scan', 'Reading every row because no index helps.']],
    quiz: [
      { q: '`WHERE email = ?` scans 50 million rows and is slow. The fix?', options: ['More RAM', 'Add an index on email', 'Only add a cache', 'Shard immediately'], answer: 1, why: 'An index turns a full scan into a quick lookup.' },
      { q: 'Why not index every column?', options: ['It’s not allowed', 'Each index slows writes and uses storage', 'It makes reads slower', 'It breaks joins'], answer: 1, why: 'Every insert and update must also update every index.' },
      { q: 'Which structure do most relational databases use for indexes?', options: ['A hash map only', 'A B-tree', 'A linked list', 'A Bloom filter'], answer: 1, why: 'B-trees support both exact lookups and range scans.' },
    ],
    refs: ['luke', 'ddia'],
  },
  replication: {
    analogy: 'A teacher (leader) writes notes on the board; students (followers) copy them into their notebooks. Anyone can read from a notebook, but a student who writes slowly may be a few lines behind.',
    what: 'Replication keeps copies of the same data on several machines.',
    why: 'If one machine dies, another has the data (availability). Copies can also serve reads, so you handle more traffic.',
    how: 'Usually one leader accepts writes and streams changes to followers. Synchronous copying is safer but slower; asynchronous is fast but followers can lag, so reads may be slightly old.',
    terms: [['Leader / primary', 'The node that accepts writes.'], ['Follower / replica', 'A node that copies the leader and can serve reads.'], ['Replication lag', 'How far behind a follower is.'], ['Quorum', 'A majority of replicas that must agree.']],
    quiz: [
      { q: 'The leader dies with asynchronous replication. What can be lost?', options: ['Nothing', 'The most recent writes not yet copied to followers', 'All the data', 'Only the indexes'], answer: 1, why: 'Async acknowledges before followers have the data.' },
      { q: 'Why add read replicas?', options: ['To speed up writes', 'To spread read traffic and survive a failure', 'To reduce storage', 'To replace backups'], answer: 1, why: 'Reads scale out; writes still go to the leader.' },
      { q: 'Replicas vs backups — why do you need both?', options: ['They’re the same thing', 'An accidental delete replicates instantly; only a backup lets you go back in time', 'Backups are faster', 'Replicas are cheaper'], answer: 1, why: 'Replication protects against machine failure, not human mistakes.' },
    ],
    refs: ['ddia', 'dynamo'],
  },
  sharding: {
    analogy: 'A huge phone book split into volumes A–F, G–M and so on, each kept by a different librarian. Nobody holds the whole book, but you need to know which volume to ask.',
    what: 'Sharding (partitioning) splits one big dataset into pieces, each stored on a different machine.',
    why: 'When data or write traffic is too big for one machine, splitting it is the only way to keep scaling.',
    how: 'Choose a shard key (such as user_id) and a rule that maps each key to a shard — by range, by hash, or via a lookup table. Queries that include the key go to one shard; others must ask them all.',
    terms: [['Shard key', 'The field that decides where a row lives.'], ['Hot spot', 'One shard receiving far more traffic than the others.'], ['Scatter-gather', 'Sending a query to all shards and combining the results.'], ['Resharding', 'Moving data when shards are added.']],
    quiz: [
      { q: 'You shard users by user_id. The query “find all users in Berlin” must…', options: ['Hit one shard', 'Hit every shard (scatter-gather)', 'Is impossible', 'Use only the cache'], answer: 1, why: 'City isn’t the shard key, so any shard might contain matches.' },
      { q: 'When should you shard?', options: ['Always, from day one', 'When one machine can’t handle the data size or write load', 'Never', 'Whenever reads get slow'], answer: 1, why: 'Sharding adds real complexity; first use indexes, caching and read replicas.' },
      { q: 'One celebrity’s shard gets 100× the traffic of the others. This is…', options: ['A hot spot (hot partition)', 'A cold start', 'A deadlock', 'A cache miss'], answer: 0, why: 'Fix it by splitting that key further or caching it.' },
    ],
    refs: ['ddia', 'discord', 'dynamo'],
  },
  cap: {
    analogy: 'Two bank branches whose phone line goes dead. Either both stop giving out balances until the line is back (consistent), or both keep serving customers and may disagree for a while (available).',
    what: 'CAP says that when the network between copies of your data breaks (a partition), you must choose between consistency (everyone sees the latest data) and availability (every request gets an answer).',
    why: 'Network failures do happen. Deciding in advance what the system should do during one is a core design choice.',
    how: 'For money, inventory and locks, choose CP: refuse requests that can’t be answered correctly. For carts, likes and feeds, choose AP: keep answering and reconcile later.',
    terms: [['Partition', 'A network failure that splits nodes into groups that can’t talk to each other.'], ['CP', 'Stays correct, sacrifices some availability during a partition.'], ['AP', 'Stays available, may return stale or conflicting data.'], ['PACELC', 'Even without partitions, there’s a latency vs consistency trade-off.']],
    quiz: [
      { q: 'A bank balance service during a network partition should be…', options: ['AP — always answer', 'CP — refuse rather than show a wrong balance', 'Turned off entirely', 'Served from cache only'], answer: 1, why: 'A wrong balance is worse than a short error.' },
      { q: 'A shopping cart during a partition should be…', options: ['CP — reject adds', 'AP — accept adds and merge carts later', 'Deleted', 'Frozen'], answer: 1, why: 'Losing a sale is worse than a duplicated item that gets merged (Amazon’s Dynamo made exactly this choice).' },
      { q: 'Why can’t a distributed system just “pick CA”?', options: ['It can', 'Network partitions happen anyway, so P isn’t optional', 'CA is too expensive', 'Only NoSQL can'], answer: 1, why: 'The only real choice is what to give up when a partition happens.' },
    ],
    refs: ['cap12', 'pacelc', 'jepsen'],
  },
  'consistency-models': {
    analogy: 'News spreading through a town. “Strong” means everyone hears it at exactly the same moment. “Eventual” means everyone hears it at some point. “Causal” means nobody hears the reply before the question.',
    what: 'A consistency model is the promise a system makes about what reads can see after writes, when data lives on several machines.',
    why: 'Stronger promises are easier to program against but cost speed and availability. Knowing the options lets you pay only for what you need.',
    how: 'Use strong consistency for money and uniqueness, eventual for counters and feeds, and session guarantees (read-your-writes, monotonic reads) to make eventual consistency feel right to each user.',
    terms: [['Linearizable', 'Behaves like one single copy; reads always see the latest write.'], ['Eventual', 'Replicas converge if writes stop.'], ['Causal', 'Effects are never seen before their causes.'], ['Read-your-writes', 'You always see your own updates.']],
    quiz: [
      { q: 'A user posts a comment, refreshes, and it’s missing (it appears later). Which guarantee was broken?', options: ['Durability', 'Read-your-writes', 'Atomicity', 'Isolation'], answer: 1, why: 'The refresh hit a replica that hadn’t received the user’s own write yet.' },
      { q: 'Which model is the strongest?', options: ['Eventual', 'Causal', 'Linearizable', 'Read-your-writes'], answer: 2, why: 'Linearizability behaves like a single, instantly updated copy.' },
      { q: 'Why not always use the strongest model?', options: ['It’s not allowed', 'Replicas must coordinate, which costs latency and availability', 'It only uses more disk', 'It’s harder to spell'], answer: 1, why: 'Every read or write waits for agreement between machines.' },
    ],
    refs: ['jepsen', 'ddia'],
  },
  apis: {
    analogy: 'A restaurant menu: the kitchen can change how dishes are cooked, but the menu (the contract) must stay stable, or regular customers get confused.',
    what: 'An API is the contract that defines how clients and services talk — what you can ask for and what you get back.',
    why: 'Clean, stable APIs let teams and apps evolve independently. A breaking change can crash millions of old app versions.',
    how: 'Use REST for public APIs, gRPC between internal services, GraphQL for flexible frontends. Version the API, only add fields, paginate with cursors, and make unsafe operations idempotent.',
    terms: [['REST', 'Resources + HTTP verbs (GET, POST, PUT, DELETE), usually JSON.'], ['gRPC', 'Fast binary calls between services using Protocol Buffers.'], ['GraphQL', 'Clients ask for exactly the fields they need.'], ['Idempotent', 'Doing it twice has the same effect as doing it once.'], ['Cursor pagination', '“Give me the next page after item X.”']],
    quiz: [
      { q: 'A mobile app on a slow network needs data from 5 services for one screen. What helps most?', options: ['5 separate REST calls', 'One aggregated endpoint (GraphQL or a backend-for-frontend)', 'FTP', 'Polling'], answer: 1, why: 'Fewer round trips over a slow network matter more than anything else.' },
      { q: 'You must rename a field in a public API used by old app versions. Best approach?', options: ['Rename it now', 'Add the new field, keep the old one (or ship v2), then deprecate', 'Delete the API', 'Email all users'], answer: 1, why: 'Old clients keep working while new ones migrate.' },
      { q: 'Why is `?page=5000` (offset pagination) slow on huge tables?', options: ['It isn’t', 'The database still walks past all the earlier rows', 'It needs joins', 'HTTP doesn’t index offsets'], answer: 1, why: 'Cursor pagination jumps straight to “after id X” using an index.' },
    ],
    refs: ['grpc', 'stripeIdem', 'apiGateway'],
  },
  realtime: {
    analogy: 'Polling is a child asking “are we there yet?” every minute. Long polling is asking once and waiting for an answer. SSE is the driver announcing each town. WebSockets is a two-way walkie-talkie.',
    what: 'Real-time techniques let the server push new data to clients as soon as it happens, instead of waiting for the client to ask.',
    why: 'Chat, live scores, notifications and collaborative editing feel broken if updates lag by seconds.',
    how: 'Pick the simplest option that fits: SSE for one-way server-to-client updates, WebSockets for two-way messaging. Track which server holds each connection, and use pub/sub to route messages between servers.',
    terms: [['Polling', 'The client asks repeatedly at fixed intervals.'], ['Long polling', 'The server holds the request open until there’s news.'], ['SSE', 'Server-Sent Events: a one-way stream over HTTP.'], ['WebSocket', 'A persistent two-way connection.'], ['Pub/sub', 'Publishers send to a topic; all subscribers receive it.']],
    quiz: [
      { q: 'A chat app needs both sides sending messages instantly. Best choice?', options: ['Short polling', 'SSE', 'WebSockets', 'Email'], answer: 2, why: 'Two-way, low-latency messaging is what WebSockets are for.' },
      { q: 'Long polling means…', options: ['The client polls every second', 'The server holds the request open until there’s new data', 'UDP streaming', 'A WebSocket'], answer: 1, why: 'It avoids empty responses, at the cost of one request per message.' },
      { q: '10 million users connected by WebSocket, 100,000 connections per server. How many servers?', options: ['10', '100', '1,000', '10,000'], answer: 1, why: '10,000,000 ÷ 100,000 = 100 (plus spare capacity).' },
    ],
    refs: ['mdnWs', 'mdnSse', 'whatsapp'],
  },
  queues: {
    analogy: 'The order rail in a restaurant kitchen. Waiters clip orders on; cooks take them off when they’re free. A rush of orders just makes the rail longer — nobody is turned away, and cooks work at their own pace.',
    what: 'A message queue is a buffer between parts of a system: producers put messages in, consumers take them out and process them later.',
    why: 'It decouples services (the sender doesn’t wait), absorbs traffic spikes, and lets failed work be retried safely.',
    how: 'Put slow or bursty work (emails, video processing, fan-out) on a queue. Workers consume it, acknowledge when done, and retry on failure. Watch queue depth and add consumers when it grows.',
    terms: [['Producer / consumer', 'Who puts messages in / who takes them out.'], ['Ack', 'The consumer confirming a message is done, so it can be removed.'], ['Dead-letter queue', 'Where messages go after repeatedly failing.'], ['Consumer lag', 'How far behind consumers are.'], ['Backpressure', 'Slowing producers down when consumers can’t keep up.']],
    quiz: [
      { q: 'Signup sends a welcome email that takes 2 seconds. How do you keep signup fast?', options: ['A faster email server', 'Put a “send email” job on a queue; a worker sends it', 'Skip the email', 'A bigger web server'], answer: 1, why: 'The user gets an instant response; the email follows moments later.' },
      { q: 'One message crashes the consumer every time. Without a dead-letter queue…', options: ['It’s deleted automatically', 'It’s retried forever, blocking or wasting consumers', 'Kafka fixes it', 'Nothing happens'], answer: 1, why: 'This is a “poison message”; a DLQ sets it aside for inspection.' },
      { q: 'Queue depth keeps growing all day. What does that mean?', options: ['All is well', 'Consumers are slower than producers — add consumers or slow producers', 'The queue is broken', 'There are too many consumers'], answer: 1, why: 'Depth only grows when input rate > processing rate.' },
    ],
    refs: ['kafka', 'ddia', 'primer'],
  },
  microservices: {
    analogy: 'A big company splitting into departments (billing, shipping, support). Each can hire and change its processes independently, but now they need memos, meetings and clear rules to cooperate.',
    what: 'Microservices split an application into small services, each owning one business capability and its own data, communicating over the network.',
    why: 'Teams can deploy and scale their service independently, and a failure in one area doesn’t take down everything.',
    how: 'Put an API gateway in front for auth, routing and rate limiting. Services find each other through service discovery, talk via APIs or events, and never share a database. Start with a modular monolith; split when there’s a clear reason.',
    terms: [['Monolith', 'One deployable application containing all features.'], ['API gateway', 'The single entry point that routes requests to services.'], ['Service discovery', 'Finding the current addresses of healthy service instances.'], ['BFF', 'Backend-for-frontend: a gateway tailored to one client type.']],
    quiz: [
      { q: 'A startup with 3 engineers is building a new product. Best architecture?', options: ['50 microservices', 'A well-structured monolith', 'Serverless only', 'A mainframe'], answer: 1, why: 'Microservices solve team and scaling problems they don’t have yet — and add a lot of overhead.' },
      { q: 'Service A calls B calls C calls D, each 99.9% available. Overall availability?', options: ['99.9%', 'About 99.6%', '99.99%', '100%'], answer: 1, why: '0.999⁴ ≈ 0.996 — each hop in a chain lowers availability.' },
      { q: 'Why shouldn’t two services share one database?', options: ['It’s too expensive', 'It couples them — a schema change in one can break the other', 'Databases can’t be shared', 'It’s slower'], answer: 1, why: 'Shared data undoes the independence that microservices are for.' },
    ],
    refs: ['fowlerMs', 'apiGateway', 'sdi2'],
  },
  'rate-limiting': {
    analogy: 'A ticket machine at a museum: tickets refill every few seconds, and you can only enter with a ticket. A group can use the saved-up tickets at once, then everyone waits for new ones.',
    what: 'Rate limiting caps how many requests a client can make in a period of time.',
    why: 'It protects services from overload, abuse (scraping, password guessing) and runaway bugs in client code, and keeps usage fair between customers.',
    how: 'Identify the caller (API key, user, IP). Count requests with an algorithm such as token bucket or sliding window, stored in a fast shared store like Redis. Reject extra requests with HTTP 429 and a Retry-After header.',
    terms: [['Token bucket', 'Tokens refill at a steady rate; each request spends one.'], ['Fixed window', 'Count requests per clock minute.'], ['Sliding window', 'Count requests in the last N seconds, rolling.'], ['429', 'HTTP status “Too Many Requests”.']],
    quiz: [
      { q: 'Token bucket: capacity 10, refill 1/s, bucket full. A client sends 15 requests at once. How many are accepted?', options: ['1', '10', '15', '0'], answer: 1, why: 'The 10 saved tokens are spent; the other 5 are rejected until tokens refill.' },
      { q: 'Which HTTP status means “rate limited”?', options: ['200', '404', '429', '500'], answer: 2, why: '429 Too Many Requests, ideally with Retry-After.' },
      { q: 'Why rate-limit login attempts per account?', options: ['To save bandwidth', 'To stop password-guessing (brute-force) attacks', 'To make logins faster', 'HTTP requires it'], answer: 1, why: 'Limits make guessing millions of passwords impractical.' },
    ],
    refs: ['stripeRl', 'sdi1'],
  },
  resilience: {
    analogy: 'An electrical fuse: when one appliance shorts, the fuse trips so the whole house doesn’t catch fire. You reset it once the fault is fixed.',
    what: 'Resilience patterns — timeouts, retries, circuit breakers and bulkheads — stop one failing part from dragging the whole system down.',
    why: 'In a distributed system something is always slow or broken. Without guards, one slow dependency ties up every thread and the failure cascades.',
    how: 'Give every network call a timeout. Retry only safe (idempotent) operations, with exponential backoff and jitter. Wrap flaky dependencies in a circuit breaker, isolate resources per dependency, and degrade gracefully.',
    terms: [['Timeout', 'The maximum time to wait for a response.'], ['Backoff', 'Waiting longer between each retry.'], ['Jitter', 'Adding randomness to the wait so clients don’t retry in sync.'], ['Circuit breaker', 'Stops calling a failing service for a while.'], ['Bulkhead', 'Separate resource pools, so one failure can’t exhaust everything.']],
    quiz: [
      { q: 'A dependency hangs. Without timeouts, what happens to your service?', options: ['Nothing', 'Threads wait forever and pile up until your service fails too', 'It speeds up', 'It heals itself'], answer: 1, why: 'That’s a cascading failure.' },
      { q: 'Which operation is safe to retry blindly?', options: ['POST /payments without an idempotency key', 'GET /profile', 'POST /send-sms', 'Transfer money'], answer: 1, why: 'Reading has no side effects; the others could happen twice.' },
      { q: 'The recommendations service is down. The product page should…', options: ['Return a 500 error', 'Show the page without recommendations (graceful degradation)', 'Retry forever', 'Crash'], answer: 1, why: 'Non-essential features should fail quietly.' },
    ],
    refs: ['awsRetries', 'fowlerCb', 'sre'],
  },
  observability: {
    analogy: 'A car dashboard (metrics) tells you something’s wrong, the service logbook (logs) records what happened, and a GPS trace of a journey (tracing) shows exactly where you got stuck.',
    what: 'Observability is being able to understand what your system is doing from the outside, using metrics, logs and traces.',
    why: 'You can’t fix what you can’t see. At 3 AM during an outage you need to find the broken part in minutes, not hours.',
    how: 'Emit metrics (rates, errors, latency percentiles), structured logs with request IDs, and distributed traces across services. Build dashboards, and alert on user-facing symptoms such as SLO burn rate.',
    terms: [['Metric', 'A number over time, e.g. requests per second.'], ['Log', 'A timestamped record of an event.'], ['Trace', 'The path and timing of one request through all services.'], ['RED', 'Rate, Errors, Duration — the key service metrics.']],
    quiz: [
      { q: 'Users say checkout is sometimes slow. What best shows WHICH service made a slow request slow?', options: ['A CPU graph', 'A distributed trace', 'Grepping error logs', 'An uptime check'], answer: 1, why: 'A trace breaks one request down hop by hop.' },
      { q: 'RED stands for…', options: ['Read, Execute, Delete', 'Rate, Errors, Duration', 'Redundancy, Elasticity, Durability', 'Replication, Election, Distribution'], answer: 1, why: 'The three numbers to watch for every service.' },
      { q: 'Why write structured (JSON) logs?', options: ['They look nicer', 'Machines can search and filter by field', 'They’re smaller', 'The law requires it'], answer: 1, why: '“All errors for user 42 in the last hour” becomes a quick query.' },
    ],
    refs: ['otel', 'sreMonitoring'],
  },
  consensus: {
    analogy: 'A group of friends choosing a restaurant by majority vote. Even if two friends’ phones die, the rest can still decide — and nobody can go ahead with a choice the majority didn’t agree to.',
    what: 'Consensus is how a group of machines agrees on a single value or order of events, even when some of them fail.',
    why: 'Leader election, locks and configuration need exactly one answer. Without proper consensus you get “split brain”: two leaders both accepting writes.',
    how: 'Algorithms like Raft elect a leader by majority vote, and the leader commits changes only once a majority has stored them. In practice you use etcd, ZooKeeper or Consul rather than writing it yourself.',
    terms: [['Quorum', 'A majority of nodes, e.g. 3 of 5.'], ['Term', 'An election period in Raft.'], ['Split brain', 'Two nodes both acting as leader.'], ['Fencing token', 'An increasing number that stops an old leader from doing damage.']],
    quiz: [
      { q: 'Why run 3 or 5 consensus nodes, not 4?', options: ['4 is unlucky', '4 tolerates only 1 failure — the same as 3 — at higher cost', '4 is slow to boot', 'No reason'], answer: 1, why: 'A majority of 4 is 3, so only one node can fail.' },
      { q: 'In Raft, the leader stops sending heartbeats. The followers…', options: ['Wait forever', 'Time out and start an election', 'Shut down', 'All become leaders'], answer: 1, why: 'Randomised timeouts make one candidate usually win quickly.' },
      { q: 'A typical use of etcd or ZooKeeper is…', options: ['Storing videos', 'Leader election and configuration', 'Full-text search', 'Serving as a CDN'], answer: 1, why: 'Small, critical, strongly consistent data.' },
    ],
    refs: ['raftViz', 'raft', 'ddia'],
  },
  'distributed-transactions': {
    analogy: 'Booking a holiday: flight, hotel and car from three different companies. If the car is unavailable you can’t “undo time” — you cancel the hotel and refund the flight. That sequence of steps with cancellations is a saga.',
    what: 'A distributed transaction keeps data consistent when one business action changes data in several services or databases.',
    why: 'Each service has its own database, so no single transaction can cover them all. Without a plan, a failure midway leaves money taken but no order.',
    how: 'Use a saga: a series of local steps, each with a compensating “undo” step. Use the transactional outbox so each database change and its event are saved together. Two-phase commit is only for tightly coupled systems.',
    terms: [['2PC', 'Two-phase commit: everyone prepares, then everyone commits.'], ['Saga', 'A sequence of local transactions with compensations.'], ['Compensation', 'The step that semantically undoes an earlier step (refund, release stock).'], ['Outbox', 'A table where events are written in the same transaction as the data change.']],
    quiz: [
      { q: 'Booking a hotel and a flight through two companies’ APIs. Best approach?', options: ['2PC across both companies', 'A saga with compensations (cancel the hotel if the flight fails)', 'Hope it works', 'Put both in one database'], answer: 1, why: 'You can’t hold locks inside another company’s system.' },
      { q: 'In the outbox pattern, the event is first written…', options: ['Directly to Kafka', 'To an outbox table, in the same DB transaction as the data change', 'To a file', 'To the cache'], answer: 1, why: 'Either both are saved or neither; a relay publishes it afterwards.' },
      { q: 'During a saga, another user can see “payment pending”. This is…', options: ['Always a bug', 'Expected — intermediate states are visible, so design the UI for them', 'Impossible', 'A deadlock'], answer: 1, why: 'Sagas trade isolation for availability and loose coupling.' },
    ],
    refs: ['saga', 'outbox', 'ddia'],
  },
  'id-generation': {
    analogy: 'Ticket numbers at a deli counter work with one machine. With 1,000 counters worldwide, each needs its own numbering scheme — say, “counter number + time + sequence” — so no two tickets ever match.',
    what: 'ID generation is creating unique identifiers for records across many machines, ideally without them having to coordinate.',
    why: 'A single auto-increment counter becomes a bottleneck and single point of failure at scale, and random IDs can hurt database performance.',
    how: 'Use Snowflake-style IDs (timestamp + machine ID + sequence), UUID v7 / ULID (time-ordered and random), or hand out blocks of IDs from a central allocator.',
    terms: [['UUID', 'A 128-bit random or time-based unique ID.'], ['Snowflake', 'A 64-bit ID: time + machine + sequence.'], ['Monotonic', 'Always increasing.'], ['Clock skew', 'Different machines’ clocks disagreeing.']],
    quiz: [
      { q: 'Why avoid exposing auto-increment IDs like /orders/1042 publicly?', options: ['They’re too long', 'They reveal your volume and are easy to guess', 'They can’t be indexed', 'They’re not unique'], answer: 1, why: 'Competitors can count your orders; attackers can enumerate records.' },
      { q: 'Snowflake IDs are roughly sortable by…', options: ['User', 'Creation time', 'Size', 'Nothing — they’re random'], answer: 1, why: 'The highest bits are a timestamp.' },
      { q: 'A server’s clock jumps backwards. A Snowflake generator should…', options: ['Keep going', 'Wait or refuse until the clock catches up, to avoid duplicates', 'Reset the sequence to 0 and continue', 'Crash the cluster'], answer: 1, why: 'Re-using past timestamps could produce the same ID twice.' },
    ],
    refs: ['snowflake', 'ddia'],
  },
};

export const CASE_EXTRA = {
  'url-shortener': {
    story: [
      'You paste a long link and click “Shorten”.',
      'The service gives the link a unique number and turns it into 7 short characters, like aZ3kQ9x.',
      'It saves “aZ3kQ9x → your long link” in a database.',
      'When anyone opens sho.rt/aZ3kQ9x, the service looks the code up — usually in a fast in-memory cache — and tells the browser “go here instead” (a redirect).',
      'Each click is also recorded in the background, so you can see stats later without slowing anyone down.',
    ],
    bigIdea: 'Links are clicked ~100× more often than they are created, so the design is all about making the lookup (read) path fast, and making sure every code is unique.',
    prereqs: ['estimation', 'load-balancing', 'caching', 'id-generation', 'sql-nosql'],
    flowIntro: { create: 'What happens when someone creates a new short link.', redirect: 'What happens every time someone clicks a short link — the path that must be fastest.' },
    api: ['Creates a new short link and returns it.', 'The short link itself: sends the browser on to the original URL.', 'Returns click statistics for the link’s owner.', 'Deletes a link (owner only).'],
    data: ['One row per short link. Every lookup is “find by code”, so a key-value store is perfect.', 'One record per click, written asynchronously and analysed in bulk later.'],
    quiz: [
      { q: 'Two users shorten the same long URL. Should they get the same code?', options: ['Always', 'It’s a design choice: sharing saves space; separate codes allow per-user stats and expiry', 'It’s technically impossible', 'Only with 301 redirects'], answer: 1, why: 'Many services give each user their own code so analytics and deletion are per user.' },
      { q: 'Where can redirects be cached to cut latency most for users around the world?', options: ['Only in the database', 'An in-memory cache plus edge caching at a CDN', 'In the browser only', 'Nowhere'], answer: 1, why: 'Edge caching answers from a city near the user.' },
      { q: 'Which store fits the `urls` table best?', options: ['Graph database', 'Key-value / wide-column store — single-key lookups at huge scale', 'Search index', 'Spreadsheet'], answer: 1, why: 'No joins, just get-by-key: the ideal key-value workload.' },
    ],
    refs: ['sdi1', 'snowflake', 'primer'],
  },
  'news-feed': {
    story: [
      'You follow a few hundred accounts. When you open the app you expect their newest posts, instantly.',
      'When someone posts, the post is saved once.',
      'In the background, the system copies the post’s ID into a ready-made timeline list for each of their followers (this is called “fan-out”).',
      'When you open the app, the service simply reads your ready-made list — no searching needed — then fetches the posts and ranks them.',
      'Accounts with millions of followers are the exception: their posts are mixed in when you open the app instead, because copying them to millions of lists would be too slow.',
    ],
    bigIdea: 'Do the hard work when a post is written, so reading a timeline is cheap — except for celebrities, where that trade-off flips.',
    prereqs: ['caching', 'queues', 'sharding', 'consistency-models'],
    flowIntro: { publish: 'What happens when someone posts: the post is saved, then copied to followers’ timelines in the background.', read: 'What happens when you open the app and your timeline loads.' },
    api: ['Publishes a new post.', 'Returns one page of your home timeline, plus a cursor for the next page.', 'Follows another user.'],
    data: ['Each post is stored once, keyed by its author.', 'Who follows whom, looked up in both directions.', 'Your precomputed timeline: just a list of post IDs in memory.'],
    quiz: [
      { q: 'A user with 500 followers posts. With fan-out on write, how many timeline writes happen?', options: ['1', '500', '250,000', '0'], answer: 1, why: 'One insert per follower’s timeline.' },
      { q: 'A user hasn’t logged in for a year. Should we keep precomputing their timeline?', options: ['Yes, always', 'No — build it on demand when they return', 'Delete their account', 'Only at weekends'], answer: 1, why: 'Fan-out to inactive users wastes most of the write work.' },
      { q: 'Why store only post IDs (not whole posts) in timeline caches?', options: ['IDs look nicer', 'Far less memory; each post’s content is cached once and shared', 'Posts can’t be cached', 'Security'], answer: 1, why: '800 IDs × 8 bytes is tiny compared with 800 full posts per user.' },
    ],
    refs: ['timelines', 'sdi1', 'ddia'],
  },
  chat: {
    story: [
      'When you open the app, it opens a permanent two-way connection (a WebSocket) to one of many “gateway” servers.',
      'That gateway writes down “Alice is connected to me” in a shared registry.',
      'When Alice sends Bob a message, it travels over her connection, is saved to the database first, and she sees one tick (sent).',
      'The system looks up which gateway Bob is connected to and pushes the message down his connection; his phone confirms, and Alice sees two ticks (delivered).',
      'If Bob is offline, he gets a push notification instead, and when his app reconnects it downloads everything he missed.',
    ],
    bigIdea: 'Keep a live connection to every online user, always save a message before delivering it, and know which server each user is connected to.',
    prereqs: ['realtime', 'load-balancing', 'caching', 'queues', 'consistency-models'],
    flowIntro: { connect: 'What happens when the app opens: a long-lived connection is set up and registered.', online: 'Sending a message when the recipient is online.', offline: 'Sending a message when the recipient is offline.' },
    api: ['Opens the long-lived two-way connection.', 'A message sent over that connection, and the acknowledgement that comes back.', 'Downloads messages missed while offline.'],
    data: ['All messages in a conversation are stored together, in order.', 'Which gateway server each online user is connected to; expires automatically if heartbeats stop.', 'Per device, the last message it has received — used to sync after reconnecting.'],
    quiz: [
      { q: 'Why save the message before delivering it?', options: ['It’s faster', 'So it isn’t lost if a server crashes mid-delivery', 'WebSockets require it', 'For encryption'], answer: 1, why: 'Durability first: once it’s saved, it can always be delivered later.' },
      { q: 'Bob has a phone and a laptop. How does each get the message?', options: ['Only the phone does', 'Each device has its own connection and delivery cursor', 'The laptop asks the phone', 'By email'], answer: 1, why: 'Multi-device means tracking delivery per device.' },
      { q: 'Why not broadcast every “online / offline” change to all 500 contacts instantly?', options: ['Only for privacy', 'It creates huge fan-out traffic; send it lazily, only to people viewing the chat', 'It’s impossible', 'It costs nothing'], answer: 1, why: 'Presence updates can be the largest source of traffic in a chat system.' },
    ],
    refs: ['sdi1', 'whatsapp', 'discord'],
  },
  'video-streaming': {
    story: [
      'A creator uploads a video straight into cloud storage, in chunks, so a dropped connection doesn’t mean starting again.',
      'A queue of jobs tells worker machines to process it: the video is cut into 4-second pieces, and each piece is converted into several qualities (240p to 4K) in parallel.',
      'The pieces and a “playlist” file listing them are saved, and the video is marked ready.',
      'When you press play, your player downloads the playlist, then fetches the pieces one by one from a CDN server near you.',
      'Your player keeps measuring your internet speed and picks the best quality for each next piece — that’s why quality drops briefly on a slow connection instead of stopping.',
    ],
    bigIdea: 'Do the heavy processing once at upload time, and serve the result from caches close to viewers, because bandwidth is the biggest cost.',
    prereqs: ['cdn', 'queues', 'estimation', 'caching'],
    flowIntro: { upload: 'What happens after a creator uploads: storage, a job queue and parallel processing.', watch: 'What happens when a viewer presses play.' },
    api: ['Starts an upload and returns temporary URLs to upload directly to storage.', 'Tells the service the upload is finished, so processing can begin.', 'Returns a video’s details and its playlist URL.', 'One 4-second piece of video, served by the CDN.'],
    data: ['One row per video: title, owner, status, and which qualities exist.', 'The actual video files: the original upload, every processed piece, and the playlists.'],
    quiz: [
      { q: 'Why split videos into ~4-second segments?', options: ['Shorter file names', 'It enables parallel processing, CDN caching and switching quality mid-video', 'MP4 requires it', 'To hide ads'], answer: 1, why: 'Small independent pieces make every stage easier.' },
      { q: 'A viewer switches from Wi-Fi to a weak mobile signal mid-video. With adaptive bitrate…', options: ['The video stops', 'The player picks a lower quality for the next segments', 'The server re-encodes the video', 'Nothing changes'], answer: 1, why: 'Lower quality keeps playback smooth; it climbs back up when the signal improves.' },
      { q: 'Where are the video bytes served from for most views?', options: ['App servers', 'The database', 'CDN edge caches near viewers', 'The transcoders'], answer: 2, why: 'Serving from edges is faster and far cheaper.' },
    ],
    refs: ['hls', 'openconnect', 'sdi1'],
  },
  'ride-hailing': {
    story: [
      'Every online driver’s phone sends its location every few seconds.',
      'The system keeps these positions in memory, organised by map area (small hexagons), so “who is near here?” is quick to answer.',
      'When you request a ride, the system estimates the price, then looks for available drivers in your area and nearby areas, ranked by how long they would take to reach you by road.',
      'It offers the trip to the best driver and locks that driver, so no other rider can get them at the same time.',
      'When the driver accepts, you see them approaching live on the map.',
    ],
    bigIdea: 'Handle an enormous stream of throwaway location updates in memory, but make the “assign this driver” step strictly consistent.',
    prereqs: ['sharding', 'caching', 'consistency-models', 'realtime'],
    flowIntro: { location: 'How drivers’ positions are continuously updated.', request: 'What happens when a rider requests a ride and gets matched.' },
    api: ['A driver’s current position, sent every few seconds.', 'A rider requests a trip; returns a price estimate and the trip ID.', 'A driver accepts a trip offer (fails if another driver already took it).'],
    data: ['One row per trip, moving through clear states from REQUESTED to COMPLETED.', 'Where each driver is right now, grouped by map cell; entries expire if not refreshed.'],
    quiz: [
      { q: 'Why rank drivers by ETA rather than straight-line distance?', options: ['It’s cheaper', 'A driver across a river may look close but be 20 minutes away by road', 'The law requires it', 'ETAs are random'], answer: 1, why: 'What the rider cares about is pickup time.' },
      { q: 'A driver taps “accept” but the network drops and the app retries. Why must “accept” be idempotent?', options: ['For speed', 'So the retry doesn’t double-assign or fail confusingly', 'Encryption', 'Billing'], answer: 1, why: 'The same accept, repeated, should have the same single effect.' },
      { q: 'Why shard the system by city or region?', options: ['For tax', 'Most queries are local, and failures stay contained to one region', 'Disks are cheaper', 'GPS requires it'], answer: 1, why: 'A rider in Paris never needs drivers in Tokyo.' },
    ],
    refs: ['h3', 'sdi2', 'ddia'],
  },
  notifications: {
    story: [
      'Other parts of the business (orders, security, marketing) ask the notification service to “tell user 42 their order has shipped”.',
      'The service checks the user’s preferences: have they opted out? Is it the middle of the night where they live? Which phone and email do they have?',
      'It drops duplicates and enforces limits, so nobody gets spammed.',
      'The notification is put on a queue for each channel (push, SMS, email), with urgent ones like login codes jumping ahead.',
      'Workers send them through outside providers (Apple, Google, SMS and email companies), retrying if a provider fails, and record what happened.',
    ],
    bigIdea: 'Queues, retries and de-duplication make delivery reliable even though every provider you rely on sometimes fails.',
    prereqs: ['queues', 'resilience', 'rate-limiting', 'apis'],
    flowIntro: { send: 'The normal path, from request to delivery.', failure: 'What happens when an outside provider fails.' },
    api: ['Asks for a notification to be sent to a user.', 'Updates a user’s notification preferences.'],
    data: ['Each user’s channels, device tokens, opt-outs and quiet hours.', 'One record per notification and channel, with its delivery status.'],
    quiz: [
      { q: 'A user in Tokyo gets a marketing push at 3 AM. What check is missing?', options: ['Retries', 'Quiet hours in the user’s time zone', 'Idempotency', 'A CDN'], answer: 1, why: 'Preferences and local time should be checked before sending.' },
      { q: 'Why is SMS usually reserved for critical messages like login codes?', options: ['SMS is slow', 'Each SMS costs money, unlike push notifications', 'SMS can’t contain text', 'Apple’s rules'], answer: 1, why: 'Millions of SMS a day costs tens of thousands of dollars.' },
      { q: 'Where should providers’ rate limits be enforced?', options: ['Nowhere', 'In the channel workers, before calling the provider', 'In the client app', 'In the database'], answer: 1, why: 'Workers throttle themselves so providers don’t reject or ban them.' },
    ],
    refs: ['sdi1', 'awsRetries', 'stripeIdem'],
  },
  typeahead: {
    story: [
      'As you type “sys” into a search box, the page asks for suggestions — but waits a moment first, so it doesn’t send a request for every single key.',
      'Popular beginnings like “s” or “sy” are often answered by a cache near you.',
      'Otherwise, the suggestion service walks a tree of letters (a trie) to “s → y → s”, where the top 10 completions are already stored, and returns them in a few milliseconds.',
      'Separately, a background pipeline counts what people actually search for, favouring recent trends.',
      'Every so often, a fresh tree is built from those counts and swapped in without downtime.',
    ],
    bigIdea: 'Pre-compute the answers offline, so each keystroke is just a quick lookup in memory.',
    prereqs: ['caching', 'cdn', 'sharding', 'queues'],
    flowIntro: { query: 'What happens on each keystroke.', build: 'How suggestions are recalculated in the background.' },
    api: ['Returns the top suggestions for what has been typed so far.'],
    data: ['The letter tree held in memory; each node already knows its best 10 completions.', 'How often each query was searched recently, used to rebuild the tree.'],
    quiz: [
      { q: 'Why not run a database `LIKE \'sys%\'` query on each keystroke?', options: ['It gives wrong results', 'It’s far too slow at this volume; an in-memory trie answers instantly', 'SQL doesn’t support it', 'Security'], answer: 1, why: 'Hundreds of thousands of requests per second need memory lookups, not disk queries.' },
      { q: 'Suggestions for “new” should favour this week’s news over old queries. Which technique?', options: ['Alphabetical order', 'Time-decayed counts', 'Random order', 'Longest first'], answer: 1, why: 'Recent searches weigh more than old ones.' },
      { q: 'Why does caching short prefixes at the edge work so well?', options: ['It doesn’t', 'Millions of people type the same first letters, so the hit rate is huge', 'Browsers require it', 'Security'], answer: 1, why: 'There are only 26 one-letter prefixes and everyone uses them.' },
    ],
    refs: ['sdi1', 'primer'],
  },
  'web-crawler': {
    story: [
      'Start with a list of known websites (the seeds).',
      'A to-do list of URLs (the frontier) decides which page to fetch next — important pages first, and never too many requests to the same site at once.',
      'Fetcher machines check each site’s rules (robots.txt), download the page and hand it on.',
      'The parser saves the page (skipping near-copies) and pulls out every link.',
      'Links not seen before go back onto the to-do list — and the loop continues across billions of pages.',
    ],
    bigIdea: 'A huge, polite loop: prioritise what to fetch, never overload any website, and avoid fetching the same thing twice.',
    prereqs: ['queues', 'caching', 'sharding'],
    flowIntro: { crawl: 'One lap of the crawl loop, from picking a URL to discovering new ones.' },
    api: ['Adds a URL to the to-do list with a priority.', 'Gives a fetcher its next URL, respecting each site’s rate limit.'],
    data: ['The to-do list of URLs, grouped by website, with when each may next be fetched.', 'The downloaded pages, plus a fingerprint to spot near-duplicates.'],
    quiz: [
      { q: 'What does a website’s robots.txt tell a crawler?', options: ['The site’s password', 'Which paths it may or may not crawl', 'Its page rank', 'DNS information'], answer: 1, why: 'Respecting it is part of being a polite crawler.' },
      { q: 'A site generates endless calendar pages (“next month” forever). This is…', options: ['A CDN', 'A spider trap — limit depth and URL patterns', 'A Bloom filter', 'A feature'], answer: 1, why: 'Without limits the crawler would never escape.' },
      { q: 'Why must the frontier be stored on disk, not just in memory?', options: ['Disks are faster', 'Billions of URLs won’t fit in memory', 'Politeness', 'DNS'], answer: 1, why: 'Keep a memory buffer at the front, the bulk on disk.' },
    ],
    refs: ['sdi1', 'bloom', 'robots'],
  },
  payments: {
    story: [
      'At checkout, your card number goes directly to the payment provider (such as Stripe), which gives back a token. The shop never sees your card.',
      'The shop sends “charge this token $40” with a unique request ID (an idempotency key), so a double-click or retry can never charge you twice.',
      'The payment is recorded as “pending” before anything else, so there is always a record of what was attempted.',
      'The provider charges the card. The result is written to a ledger — a book where every movement of money is recorded as matching debits and credits.',
      'Every night, the shop compares its ledger with the provider’s report to catch anything that went wrong.',
    ],
    bigIdea: 'Correctness over speed: every step must be safe to retry, every cent recorded, and every mismatch caught by reconciliation.',
    prereqs: ['apis', 'distributed-transactions', 'sql-nosql', 'resilience', 'consistency-models'],
    flowIntro: { charge: 'The full path of charging a card safely.', recon: 'The nightly check that catches anything that slipped through.' },
    api: ['Charges a customer (safe to retry thanks to the Idempotency-Key header).', 'The payment provider telling us the final result, asynchronously.', 'Refunds all or part of a payment.'],
    data: ['One row per payment attempt and its current state.', 'Every movement of money as matching debit/credit entries; never edited, only appended.', 'Events waiting to be published, saved in the same transaction as the payment update.'],
    quiz: [
      { q: 'A user double-clicks “Pay”. What prevents two charges?', options: ['A faster button', 'The same idempotency key returns the first result', 'Rate limiting alone', 'Refunding later'], answer: 1, why: 'The second request is recognised as a repeat.' },
      { q: 'Why use a double-entry ledger instead of a single “balance” column?', options: ['It’s smaller', 'Every change is recorded as balanced entries, so balances are auditable and errors stand out', 'It’s faster', 'SQL requires it'], answer: 1, why: 'You can always explain a balance from its history.' },
      { q: 'Reconciliation finds our ledger says “paid” but the provider says “failed”. Next step?', options: ['Ignore it', 'Investigate and post correcting entries based on the provider’s settlement report', 'Delete the ledger', 'Automatically charge again'], answer: 1, why: 'The settlement report shows what money actually moved.' },
    ],
    refs: ['stripeIdem', 'accounting', 'outbox', 'sdi2'],
  },
};
